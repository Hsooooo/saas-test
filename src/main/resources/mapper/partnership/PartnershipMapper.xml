<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.illunex.emsaasrestapi.partnership.mapper.PartnershipMapper">

    <select id="selectByDomain" resultType="PartnershipVO">
        SELECT idx,
               name,
               domain,
               image_url,
               image_path,
               comment,
               update_date,
               create_date
          FROM partnership
         WHERE domain = #{domain}
    </select>

    <select id="selectByIdx" resultType="PartnershipVO">
        SELECT idx,
               name,
               domain,
               image_url,
               image_path,
               comment,
               update_date,
               create_date
          FROM partnership
         WHERE idx = #{idx}
    </select>

    <insert id="insertByPartnershipVO" parameterType="PartnershipVO" useGeneratedKeys="true" keyProperty="idx">
        INSERT INTO partnership(
            name,
            domain,
            image_url,
            image_path,
            comment,
            update_date,
            create_date
        ) VALUES (
            #{name},
            #{domain},
            #{imageUrl},
            #{imagePath},
            #{comment},
            now(),
            now()
        )
    </insert>

    <select id="selectByMember" resultType="PartnershipVO">
        SELECT p.idx,
               p.name,
               p.domain,
               p.image_url,
               p.image_path,
               p.comment,
               p.update_date,
               p.create_date
          FROM partnership p
          JOIN partnership_member pm ON p.idx = pm.partnership_idx
         WHERE pm.member_idx = #{memberIdx}
    </select>


</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.illunex.emsaasrestapi.member.mapper.MemberMapper">


    <select id="selectByEmail" resultType="MemberVO">
        SELECT idx,
               email,
               name,
               password,
               type_cd,
               state_cd,
               comment,
               last_login_date,
               leave_date,
               update_date,
               create_date
         FROM member
        WHERE email = #{email}
    </select>

    <update id="updateNameByIdx">
        UPDATE member
           SET name = #{name}
         WHERE idx = #{idx}
    </update>

    <select id="selectByIdx" resultType="MemberVO">
        SELECT idx,
               email,
               name,
               password,
               type_cd,
               state_cd,
               comment,
               last_login_date,
               leave_date,
               update_date,
               create_date
        FROM member
        WHERE idx = #{idx}
    </select>

    <update id="updateMemberStateByIdx">
        UPDATE member
          SET state_cd = #{stateCd}
         WHERE idx = #{idx}
    </update>

    <update id="updateStateAndPasswordByIdx">
        UPDATE member
        SET state_cd = #{stateCd},
            password = #{password}
        WHERE idx = #{idx}
    </update>

</mapper>
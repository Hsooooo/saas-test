<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.illunex.emsaasrestapi.member.mapper.MemberJoinMapper">

    <insert id="insertByMemberJoin" parameterType="MemberVO" useGeneratedKeys="true" keyProperty="idx">
        INSERT INTO member(
           email,
           password,
           profile_image_url,
           type_cd,
           state_cd,
           comment,
           update_date,
           create_date
        ) VALUES (
            #{email},
            #{password},
            #{profileImageUrl},
            #{typeCd},
            #{stateCd},
            #{comment},
            now(),
            now()
        )
    </insert>


</mapper>
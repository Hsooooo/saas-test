<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.illunex.emsaasrestapi.partnership.mapper.PartnershipInvitedMemberMapper">

    <insert id="insertInvitedMember" parameterType="PartnershipInvitedMemberVO" useGeneratedKeys="true" keyProperty="idx">
        INSERT INTO partnership_invited_member (
            email,
            partnership_idx,
            invited_by_partnership_member_idx,
            member_idx,
            partnership_member_idx,
            invited_date,
            joined_date
        )
        VALUES (
           #{email},
           #{partnershipIdx},
           #{invitedByPartnershipMemberIdx},
           #{memberIdx},
           #{partnershipMemberIdx},
           now(),
           #{joinedDate}
       )
    </insert>


    <select id="existsInvitedMember" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM partnership_invited_member
            WHERE partnership_idx = #{partnershipIdx}
              AND email = #{email}
        )
    </select>

    <select id="selectAllByPartnershipIdx" resultType="PartnershipInvitedMemberVO">
        SELECT
            idx,
            email,
            partnership_idx,
            invited_by_partnership_member_idx,
            member_idx,
            partnership_member_idx,
            invited_date,
            joined_date
        FROM partnership_invited_member
        WHERE partnership_idx = #{partnershipIdx}
    </select>

    <select id="selectByPartnershipMemberIdx" resultType="PartnershipInvitedMemberVO">
        SELECT
            idx,
            email,
            partnership_idx,
            invited_by_partnership_member_idx,
            member_idx,
            partnership_member_idx,
            invited_date,
            joined_date
        FROM partnership_invited_member
        WHERE partnership_member_idx = #{partnershipMemberIdx}
    </select>

    <update id="updateJoinedDateNowByIdx">
        UPDATE partnership_invited_member
        SET joined_date = now()
        WHERE idx = #{idx}
    </update>

</mapper>
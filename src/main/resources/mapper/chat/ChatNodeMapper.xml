<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.illunex.emsaasrestapi.chat.mapper.ChatNodeMapper">

    <insert id="insertBulkNode">
        INSERT INTO chat_node (chat_network_idx, id, labels, properties, create_date, update_date)
        VALUES
        <foreach collection="list" item="n" separator=",">
            (#{n.chatNetworkIdx}, #{n.id}, #{n.labels}, #{n.properties}, NOW(), NOW())
        </foreach>
    </insert>

    <select id="selectByChatNetworkIdxIn" resultType="ChatNodeVO">
        SELECT *
        FROM chat_node
        WHERE chat_network_idx IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>
